# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.ruff_cache


# Virtual environments
.venv
venv/

# Secrets — .env holds real credentials and must never be committed.
# .env.example is the checked-in template and is intentionally NOT ignored.
.env
.env.*
!.env.example
